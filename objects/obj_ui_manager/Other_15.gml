/// @description Set layer visibility
// By default, only leave border_layer enabled (it's the only thing that doesn't change).
layer_set_visible(main_layer, false);
layer_set_visible(pause_layer, false);
layer_set_visible(controls_layer, false);
layer_set_visible(border_layer, false);